{
  "name": "membership",
  "version": "1.0.0",
  "description": "``` 用户 → 前端 → Cloudflare Worker → KV 存储       ↓   会员文章（加密）→ 验证 → 解密 → 显示 ```",
  "main": "generate-code.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wrangler": "^4.110.0"
  }
}
